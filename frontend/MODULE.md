# Module Frontend (Pet Filter UI)

## Nghiệp vụ
Hiển thị UI tối giản, bắt hình ảnh từ Webcam.

## Logic
1. Giao diện CSS `glassmorphism` hoặc `minimal`.
2. Bắt ảnh gửi qua API mỗi 1.5s.
3. Thay đổi màu sắc thẻ Card (`safe` = Xanh, `alarm` = Đỏ) tùy theo kết quả JSON.
